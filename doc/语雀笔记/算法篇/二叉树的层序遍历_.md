# 二叉树的层序遍历_

> 给你一个二叉树，请你返回其按 层序遍历 得到的节点值。 （即逐层地，从左到右访问所有节点）。
>
> 示例：
> 二叉树：[3,9,20,null,null,15,7],
>
> 返回其层次遍历结果：
>
> [
> [3],
> [9,20],
> [15,7]
> ]

```plain
 	3
   / \
  9  20
    /  \
   15   7
```

### 1.解决思路

> 维护一个队列
>
> 1. 根节点入队
> 2. 遍历队列，直到队列为空
> 3. 记录此时队列长度，循环n次
>
>    - 取出对头节点，将节点值输出
>    - 判断当前节点是否有左右节点，如果有则入队
> 4. 重复2、3

#### 1.2Java代码

```java
List<Integer> res = new ArrayList<>();

public static void solution(TreeNode root) {
    Queue<TreeNode> queue = new ArrayDeque<>();
    queue.add(root);
    while (!queue.isEmpty()) {
        List<Integer> temp = new ArrayList<>();
        int size = queue.size();
        // 第0层一定循环1次，第n层最多循环2^n次
        for (int i = 0; i < size; i++) {
            TreeNode p = queue.poll();
            temp.add(p.val);
            if (p.left != null) queue.add(p.left);
            if (p.right != null) queue.add(p.right);
        }
        res.add(temp);
    }
}
```

#### 1.2示意图

![](assets/network-asset-20200611152404-20250108100927-1pdkc4v.gif)

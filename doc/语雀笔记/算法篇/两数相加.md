# 两数相加

> 给出两个 非空 的链表用来表示两个非负的整数。其中，它们各自的位数是按照 逆序 的方式存储的，并且它们的每个节点只能存储 一位 数字。
>
> 如果，我们将这两个数相加起来，则会返回一个新的链表来表示它们的和。
>
> 您可以假设除了数字 0 之外，这两个数都不会以 0 开头。
>
> 示例：
>
> 输入：(2 -> 4 -> 3) + (5 -> 6 -> 4)
>
> 输出：7 -> 0 -> 8
>
> 原因：342 + 465 = 807

### 1.解决思路

> 定义一个变量curry为进位值，初始值为0。遍历两个链表，如果当前节点不为空则累加值到sum。将curry的值加到sum，计算curry值为sum / 10，生成新的节点new ListNode(sum % 10)

#### 1.1Java代码

```java
static ListNode addTwoNumbers(ListNode head1, ListNode head2) {
    ListNode dummy = new ListNode(0);
    ListNode p = dummy;
    ListNode h1 = head1;
    ListNode h2 = head2;
    int curry = 0;
    while (h1 != null || h2 != null || curry > 0) {
        int val = 0;
        if (h1 != null) {
            val += h1.val;
            h1 = h1.next;
        }
        if (h2 != null) {
            val += h2.val;
            h2 = h2.next;
        }
        val += curry;
        curry = val / 10;
        val = val % 10;
        p.next = new ListNode(val);
        p = p.next;
    }
    p.next = null;
    return dummy.next;
}
```

#### 1.2示意图

![](assets/network-asset-1591943254977-bb7ddb6f-488c-4bdb-922e-ac20de0830da-20250107175123-xwr7rlz.gif)
